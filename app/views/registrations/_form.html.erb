<%= form_with(url: sign_up_path, method: :post, local: false) do |form| %>
  <%= render layout: "shared/fieldset_component", locals: { title: "Sign Up" } do %>
    <%= form.label :email, "Email", class: "input validator" do %>
      <%= icon "envelope" %>
      <%= form.email_field :email, value: @email_hint, required: true, autofocus: true, placeholder: "your@email.com" %>
    <% end %>
    <div class="validator-hint hidden">Enter a valid email address</div>

    <%= form.label :password, "Password", class: 'input' do %>
      <%= icon "key" %>
      <%= form.password_field :password, required: true, placeholder: 'Password', autocomplete: "new-password" %>
    <% end %>

    <%= form.label :password_confirmation, "Password Confirmation", class: 'input' do %>
      <%= icon "key" %>
      <%= form.password_field :password_confirmation, required: true, placeholder: 'Confirm Password', autocomplete: "new-password" %>
    <% end %>

    <%= form.submit "Sign up", class: 'btn btn-primary' %>

    <p class="label">
      <%= link_to "Have an account already? Sign in", sign_in_path %>
    </p>
  <% end %>
<% end %>
